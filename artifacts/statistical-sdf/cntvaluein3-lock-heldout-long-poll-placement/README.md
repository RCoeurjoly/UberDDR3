# UberDDR3 SDF-Referenced Placement Features

This artifact joins normalized SDF query edges to nextpnr JSON BEL placement and reports source-to-sink Manhattan distances in same-site coordinates. The statistical columns reuse `value_ps` for compatibility with the existing ranking script; values are placement-site Manhattan distances, not picoseconds.
