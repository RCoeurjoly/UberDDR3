* Goal
Make UberDDR3 work for YPCB-00338-1P1

* Current status

** experiment/change commit discipline
Each measurement experiment or instrumentation change should be committed separately before running the hardware transaction. The commit boundary is part of the measurement record: it lets a bitstream, OpenOCD decoder, and observed JTAG output be tied back to one exact diff.

work happens in branch ypcb.
nix develop
cd examples_demo/ypcb_00338_1p1
make

Info: Max frequency for clock 'controller_clk': 95.58 MHz (PASS at 83.33 MHz)
** Success criteria
Expected good final state:

  LED2 = on
  LED1 = off
  LED0 = on

  Bad/stuck likely states:

  LED2 = off   => clock wizard did not lock, reset/clocking problem
  LED2 = on, LED1 = on, LED0 = off
               => clocks locked, but DDR3 calibration/BIST did not reach done

011
** how to program and measure
The HS3 lock must cover the whole attributable hardware transaction, not just
=openFPGALoader=. Use one locked shell that programs the bitstream and then reads
the result before releasing the lock.

Example:

=flock /tmp/ypcb-hs3-210299BF3824.lock bash -lc 'openFPGALoader -c digilent_hs3 --ftdi-serial 210299BF3824 --bitstream ypcb_00338_1p1_ddr3_openxc7.bit && openocd -c "set FTDI_SERIAL 210299BF3824" -c "set YPCB_DEBUG_SAMPLES 8" -c "set YPCB_DEBUG_DELAY_MS 250" -s /home/roland/openocd-code/tcl -f openocd_ypcb_debug.tcl'=
* Plan
** TODO_NEXT Debug ANALYZE_DQS calibration stall with JTAG.
- State "TODO_NEXT"  from              [2026-05-19 Tue 23:35]

Current hardware readback:

- =pll_locked= is 1.
- =calib_complete= is 0.
- =state= is 4, where state 4 is =ANALYZE_DQS=.

This means YPCB is not failing BIST yet. It is stuck during early MPR/DQS read
calibration, before normal memory testing starts.

Use the USER1 JTAG/BSCAN debug register as the authoritative debug path because
the YPCB LiteX platform has no known serial pin. Keep LEDs unchanged.

Expose and decode:

- =state_calibrate=
- =lane=
- =dqs_store[31:0]=
- =dqs_start_index=
- =dqs_start_index_stored=
- =dqs_start_index_repeat=
- current lane =i_phy_iserdes_dqs[lane*8 +: 8]=

Update =openocd_ypcb_debug.tcl= to print the new fields.

Rebuild and test:

- Run =make openxc7=.
- Confirm route still passes =--freq 83.333=.
- Program with:

  =flock /tmp/ypcb-hs3-210299BF3824.lock openFPGALoader -c digilent_hs3 --ftdi-serial 210299BF3824 --bitstream ypcb_00338_1p1_ddr3_openxc7.bit=

- Read multiple JTAG samples with the OpenOCD script.

Interpretation:

- =dqs_store= all zero or all one means DQS capture is dead, or polarity/IO/ISERDES is wrong.
- =dqs_store= changes but lacks =010101...= means read phase, bitslip, or MPR timing is suspect.
- The pattern exists but the state retries means stability/repeat logic or =dqs_start_index= mismatch.
- Only one lane failing points at lane pinout, DQS/DQ byte ordering, or per-lane delay.

Assumptions:

- The immediate goal is diagnosis, not a permanent user-facing debug interface.
- Seed sweep stays deferred until =dqs_store= shows what kind of failure this is.
- Similar-board comparison should focus on this calibration phase, not full BIST behavior yet.

Measurement ladder:

1. Confirm DDR3 is entering MPR read mode by instrumenting command/address during
   =MPR_READ= if DQS remains absent.
2. Measure DQS at the internal PHY boundary before assuming a controller bug.
3. Expose all byte-lane ISERDES DQS samples at once to separate no-DQS-anywhere
   from wrong-lane mapping.
4. Add per-lane event counters for nonzero and changing DQS samples during
   =COLLECT_DQS= so the diagnosis is based on repeated samples, not one instant.
5. If internal DQS is still zero, use an external scope/LA on CK, lane DQS, and
   command pins to distinguish a board/DDR command issue from a PHY capture issue.

Implemented instrumentation pass:

- USER1 JTAG is widened to 256 bits.
- The low 64 bits keep the existing status/magic/state payload.
- Bits =[127:64]= keep the current-lane DQS calibration page.
- Bits =[191:128]= expose all-lane =i_phy_iserdes_dqs[63:0]= snapshots.
- Bits =[255:192]= expose per-lane 4-bit saturating counters:
  - lower 32 bits: nonzero DQS samples during =COLLECT_DQS= or =ANALYZE_DQS=
  - upper 32 bits: changed DQS samples during =COLLECT_DQS= or =ANALYZE_DQS=

2026-05-20 instrumented hardware result:

- =make openxc7= completed and routed =i_controller_clk= at 92.64 MHz, passing
  the 83.333 MHz target.
- Programming the generated =ypcb_00338_1p1_ddr3_openxc7.bit= succeeded.
- Repeated USER1 samples after programming were stable:
  - =pll_locked=1
  - =calib_complete=0
  - low debug state read back as =state=0 in this capture
  - current-lane page: =dqs_current=0xff, =dqs_store=0x00000000=
  - all-lane PHY DQS snapshot: =all_lane_dqs=0x00ff00ff0000ffff=
  - =dqs_nonzero_nibbles=0x00000000=
  - =dqs_transition_nibbles=0x00000000=

Interpretation of this run:

- The PHY-boundary DQS snapshot is not all zero. That argues against a simple
  "no DQS anywhere" failure.
- The event counters remained zero because they only count while
  =state_calibrate == COLLECT_DQS= and =delay_before_read_data == 0=. Either the
  readback missed that window, the state machine reset/restarted before the
  sample, or the gating is too narrow for post-mortem measurement.
- Next instrumentation should latch state visits and first/last DQS values
  across =COLLECT_DQS= and =ANALYZE_DQS=, instead of relying only on live state
  and narrowly gated counters.

2026-05-20 broadened-counter hardware result:

The counter gate was broadened to count during both =COLLECT_DQS= and
=ANALYZE_DQS=. This also passed route timing: post-route =i_controller_clk= was
89.98 MHz against the 83.333 MHz target.

Repeated USER1 samples after programming the broadened probe showed:

- =pll_locked=1
- =calib_complete=0
- live states cycling through =state=3 (=COLLECT_DQS=), =state=4
  (=ANALYZE_DQS=), and =state=2= (=MPR_READ=)
- =dqs_lane=0
- =dqs_current=0xff=
- =dqs_store=0xffffffff=
- =dqs_start_index= moving, sampled at values 13, 17, 23, 27, 36, 37, 39
- =dqs_start_index_stored=39
- =all_lane_dqs=0x00ff00ff0000ffff=
- =dqs_nonzero_nibbles=0xfffff0ff=
- =dqs_transition_nibbles=0xfffff00f=

Updated interpretation:

- DQS is not absent at the internal PHY boundary. Several byte lanes are active
  and transitioning during the DQS analysis loop.
- Lane 0 is the active calibration lane in this run, and it is saturated high
  (=0xff=). The controller stores all ones in =dqs_store=, so it will never find
  the expected =0101010100= window.
- The byte-lane activity pattern is uneven: nibbles in the all-lane counters show
  inactive lanes as well as active lanes. That keeps lane mapping/pinout/polarity
  on the suspect list, but the immediate measured failure is not a missing read
  strobe; it is an all-high DQS sample pattern for the selected lane.
- Next measurement should expose the raw positive/negative DQS path or ISERDES
  bit order/polarity around lane 0, and should also expose MPR DQ data for lane
  0 at the same sample point.

Next probe implementation:

- USER1 JTAG is widened again to 384 bits.
- Bits =[319:256]= expose lane 0 MPR DQ data from the same =COLLECT_DQS= sample
  point as the lane 0 DQS capture.
- Bits =[383:320]= expose lane 0 DQS at that sample point plus its reversed,
  inverted, and inverted-reversed forms, and a saturating =COLLECT_DQS= sample
  counter.
- Hardware readback must use the locked transaction above so the programmed
  bitstream and OpenOCD samples are attributable to the same run.

2026-05-20 lane 0 DQS/MPR hardware result:

This run used one locked transaction:

1. Acquire =/tmp/ypcb-hs3-210299BF3824.lock=.
2. Program =ypcb_00338_1p1_ddr3_openxc7.bit=.
3. Run the OpenOCD USER1 readback.
4. Release the lock.

The 384-bit probe passed post-route timing: =i_controller_clk= was 93.82 MHz
against the 83.333 MHz target. Repeated samples showed:

- =pll_locked=1
- =calib_complete=0
- live states cycling through =state=2= (=MPR_READ=) and =state=4=
  (=ANALYZE_DQS=)
- live selected =dqs_lane=4 in this run
- selected-lane =dqs_current=0xff= and =dqs_store=0xffffffff=
- lane 0 same-cycle =COLLECT_DQS= sample count saturated at 255
- lane 0 DQS transforms:
  - =lane0_dqs_collect=0xff
  - =lane0_dqs_rev=0xff
  - =lane0_dqs_inv=0x00
  - =lane0_dqs_inv_rev=0x00
- lane 0 MPR DQ data from the same =COLLECT_DQS= sample:
  =lane0_mpr_dq=0xffffffffffffffff=
- all-lane DQS snapshot remained =all_lane_dqs=0x00ff00ff0000ffff=
- activity counters were =dqs_nonzero_nibbles=0xff0fffff= and
  =dqs_transition_nibbles=0xff00ffff=

Updated interpretation:

- Lane 0 is not just seeing bad DQS phase. Its MPR DQ sample is also all ones at
  the same capture point. Reversing DQS bit order does not reveal a hidden
  alternating pattern because the sampled byte is saturated.
- Since the live selected calibration lane was 4 while the fixed lane 0 page was
  all ones, the next probe should capture MPR DQ for the selected calibration
  lane, or all lanes, not only fixed lane 0.
- The all-lane DQS snapshot still shows an uneven lane pattern. That keeps
  lane mapping, byte-lane ordering, DQS polarity, and PHY input interpretation
  ahead of random timing or seed changes.


2026-05-20 selected-lane DQS/MPR hardware result:

The fixed lane 0 page was changed to capture the selected calibration lane at
the =COLLECT_DQS= sample point. The page now records:

- =selected_collect_lane=
- =selected_mpr_dq=
- =selected_dqs_collect=
- reversed, inverted, and inverted-reversed selected-lane DQS
- saturated selected-lane collect sample count

The selected-lane probe passed post-route timing: =i_controller_clk= was
86.85 MHz against the 83.333 MHz target. The hardware run used one locked
program-and-read transaction. Repeated samples showed:

- =pll_locked=1
- =calib_complete=0
- live states cycling through =state=2= (=MPR_READ=), =state=3=
  (=COLLECT_DQS=), and =state=4= (=ANALYZE_DQS=)
- selected =dqs_lane=6= and =selected_collect_lane=6=
- selected-lane =dqs_current=0xff= and =dqs_store=0xffffffff=
- selected-lane DQS transforms:
  - =selected_dqs_collect=0xff
  - =selected_dqs_rev=0xff
  - =selected_dqs_inv=0x00
  - =selected_dqs_inv_rev=0x00
- selected-lane MPR DQ data from the same =COLLECT_DQS= sample:
  =selected_mpr_dq=0xffffffffffffffff=
- selected collect sample count saturated at 255
- most all-lane snapshots were =all_lane_dqs=0x00ff00ff0000ffff=
- one live =COLLECT_DQS= sample showed =all_lane_dqs=0x00ff2aaa2a2a2a2a=
- counters were =dqs_nonzero_nibbles=0x0fffffff= and
  =dqs_transition_nibbles=0x0fffffff=

Updated interpretation:

- The failing selected calibration lane is lane 6 in this run, and lane 6 has
  both DQS and MPR DQ saturated high at the same capture point. Bit reversal or
  polarity inversion alone does not produce the expected alternating DQS
  window.
- The =0x00ff2aaa2a2a2a2a= all-lane DQS snapshot is important: several lower
  lanes show an alternating-looking =0x2a= pattern while lane 6 remains =0xff=
  and lane 7 appears =0x00=. That argues against a global absence of MPR/DQS and
  toward byte-lane mapping, pinout, lane ordering, or per-lane PHY input
  interpretation.
- Next measurement should capture all-lane MPR DQ in the same format as the
  all-lane DQS snapshot, or compare the lane order against the board XDC and the
  similar-board working target.


Hypotheses and tests:

1. Global MPR/DQS absence.
   - Prediction: all lanes should be all zero or all one, with no alternating
     looking lane anywhere.
   - Test result so far: mostly false. The live all-lane DQS sample
     =0x00ff2aaa2a2a2a2a= contains alternating-looking lower lanes.

2. DQS bit order or DQS polarity only.
   - Prediction: reversing or inverting the selected-lane DQS byte should reveal
     the expected =010101...= relationship.
   - Test result so far: false for selected lane 6. The selected DQS byte is
     =0xff=, reversed is still =0xff=, and inverted views are =0x00=.

3. Selected calibration lane is not the lane carrying valid MPR/DQS.
   - Prediction: all-lane MPR/DQ or DQS snapshots show valid-looking activity on
     lanes other than the selected failing lane.
   - Current evidence: supported by DQS. Lower lanes showed =0x2a= while selected
     lane 6 stayed =0xff=.
   - Next test implemented: USER1 widened to 448 bits with bits =[447:384]=
     holding all-lane MPR DQ burst0 from the same =COLLECT_DQS= sample. If lower
     lanes also show non-=0xff= MPR data while lane 6 is =0xff=, lane mapping or
     lane ordering becomes the primary suspect. If all lanes are =0xff=, the
     problem is more likely MPR read data capture/common DQ behavior rather than
     lane selection alone.

4. XDC byte-lane grouping or physical pinout mismatch.
   - Prediction: the board XDC's =ddr3_dq[8n+:8]= group does not correspond to
     =ddr3_dqs[n]=, or the board schematic/net names order byte lanes differently
     than the core expects.
   - Static test in progress: compare YPCB XDC byte groups and DQS pins against
     the core assumption that lane =n= is =ddr3_dq[8n+:8]= with =ddr3_dqs[n]=.

5. Per-lane PHY input interpretation issue.
   - Prediction: XDC grouping is coherent, but specific lanes still sample
     saturated values while neighboring lanes show plausible MPR/DQS.
   - Test after all-lane MPR: expose or compare raw PHY input before ISERDES /
     IDELAY for the bad lane, or try a controlled lane remap probe rather than a
     timing seed sweep.
** TODO Sweep seeds

- Run multiple =nextpnr-xilinx= seeds for the YPCB target.
- Record timing result, worst slack, and any notable placement/routing behavior
  for each seed.
- If any seed passes timing, generate its bitstream, program the board, and run
  BIST.
** TODO Compare similar boards

- Generate bitstreams for similar Kintex-7 DDR3 demo boards.
- Check whether those targets pass timing.
- Compare placements against the YPCB target, especially for DDR3, clocking, and
  delay-related resources.
- Look for placements or constraints that explain why one board succeeds or
  fails relative to another.

** DONE Try to pass timing
CLOSED: [2026-05-19 Tue 23:35]
- State "DONE"       from "TODO"       [2026-05-19 Tue 23:35]
- Build the YPCB target with the OpenXC7 clock profile.
- Inspect timing failures and determine whether they are real design issues,
  placement/routing artifacts, or tool/database limitations.
- If timing passes, program the board and run BIST.
- Record whether passing timing is sufficient for BIST to pass.
It passes timing, but not BIST.
** DONE Make it so that minimum freq is a gate
CLOSED: [2026-05-19 Tue 23:32]
- State "DONE"       from "TODO_NEXT"  [2026-05-19 Tue 23:32]
- State "TODO_NEXT"  from              [2026-05-19 Tue 23:20]
Right now it says   Info: Max frequency for clock 'i_controller_clk': 84.40 MHz (PASS at 12.00 MHz), when min should be 83.33 MHz

Info: Max frequency for clock 'i_controller_clk': 84.40 MHz (PASS at 83.33 MHz)

** DONE Build bitstream
CLOSED: [2026-05-19 Tue 23:18]
- State "DONE"       from "TODO_NEXT"  [2026-05-19 Tue 23:18]
- State "TODO_NEXT"  from              [2026-05-19 Tue 22:55]
  Info: Max frequency for clock 'i_controller_clk': 84.40 MHz (PASS at 12.00 MHz)
** DONE Keep the reproducer minimal
CLOSED: [2026-05-19 Tue 22:55]
- State "DONE"       from "TODO"       [2026-05-19 Tue 22:55]
*** DONE Minimize ypcb files [5/5]
CLOSED: [2026-05-19 Tue 22:55]
- State "DONE"       from "TODO_NEXT"  [2026-05-19 Tue 22:55]
- State "TODO_NEXT"  from              [2026-05-19 Tue 18:38]
  alinx_ax7325b is the most similar board
**** DONE ypcb_00338_1p1_ddr3.xdc
CLOSED: [2026-05-19 Tue 22:55]
- State "DONE"       from "TODO_NEXT"  [2026-05-19 Tue 22:55]
- State "TODO_NEXT"  from              [2026-05-19 Tue 18:38]
**** DONE ypcb_00338_1p1_ddr3.v
CLOSED: [2026-05-19 Tue 22:26]
- State "DONE"       from "TODO_NEXT"  [2026-05-19 Tue 22:26]
- State "TODO_NEXT"  from              [2026-05-19 Tue 18:38]
**** DONE nextpnr_clock_constraints.py
CLOSED: [2026-05-19 Tue 22:14]
- State "DONE"       from "TODO_NEXT"  [2026-05-19 Tue 22:14]
- State "TODO_NEXT"  from              [2026-05-19 Tue 18:38]
  deleted
**** DONE clk_wiz.v
CLOSED: [2026-05-19 Tue 22:14]
- State "DONE"       from "TODO_NEXT"  [2026-05-19 Tue 22:14]
- State "TODO_NEXT"  from              [2026-05-19 Tue 18:38]
**** DONE Makefile
CLOSED: [2026-05-19 Tue 22:05]
- State "DONE"       from "TODO_NEXT"  [2026-05-19 Tue 22:05]
- State "TODO_NEXT"  from              [2026-05-19 Tue 18:38]
** 2026-05-20 selected/all-lane MPR hardware result:

The next probe changed the fixed lane 0 MPR page into a selected-lane page and
added one all-lane MPR burst-0 sample from the same =COLLECT_DQS= sample point.
USER1 JTAG is now 448 bits:

- Bits =[447:384]=: all-lane MPR DQ burst 0.
- Bits =[383:320]=: selected-lane DQS sample plus reversed/inverted variants.
- Bits =[319:256]=: selected-lane MPR DQ across bursts 0..7.
- Older DQS counter, all-lane DQS, current-lane, and status pages remain.

This build passed post-route timing: =i_controller_clk= was 92.47 MHz against
the 83.333 MHz target. The hardware run used one locked transaction:

1. Acquire =/tmp/ypcb-hs3-210299BF3824.lock=.
2. Program =ypcb_00338_1p1_ddr3_openxc7.bit=.
3. Run the OpenOCD USER1 readback.
4. Release the lock.

The first post-program sample caught =state=3= (=COLLECT_DQS=):

- =pll_locked=1
- =calib_complete=0
- selected =dqs_lane=4=
- =selected_collect_lane=4=
- =selected_dqs_collect=0xff=
- =selected_dqs_rev=0xff=
- =selected_dqs_inv=0x00=
- =selected_dqs_inv_rev=0x00=
- =selected_mpr_dq=0xffffffffffffffff=
- =all_lane_mpr_burst0=0xffffffffffffffff=
- =all_lane_dqs=0x00ff00ff0000ffff=
- =dqs_store=0xf954ffff=
- =dqs_start_index=16=
- =dqs_start_index_stored=16=
- =dqs_start_index_repeat=1=

Later samples showed the controller reset/restart path with =state=0=,
=all_lane_dqs=0x00ff00ff0000ffff=, and zeroed latched MPR pages. Therefore the
first sample is the useful calibration-window measurement from this run.

Hypotheses and tests:

1. Global DQS absence.

   Test: expose all-lane =i_phy_iserdes_dqs[63:0]= and per-lane activity
   counters during =COLLECT_DQS=/=ANALYZE_DQS=.

   Result: rejected. Multiple runs saw nonzero all-lane DQS snapshots, including
   =0x00ff2aaa2a2a2a2a= and =0x00ff00ff0000ffff=. The lower-lane =0x2a= pattern
   shows MPR/DQS activity is not globally absent at the PHY boundary.

2. The problem is only fixed lane 0.

   Test: move the detailed DQS/MPR capture from fixed lane 0 to the active
   selected calibration lane.

   Result: rejected as a complete explanation. Fixed lane 0 previously captured
   =lane0_dqs_collect=0xff= and =lane0_mpr_dq=0xffffffffffffffff=. The selected
   lane run captured lane 4 with the same saturated result:
   =selected_dqs_collect=0xff= and =selected_mpr_dq=0xffffffffffffffff=.

3. Simple DQS polarity or bit-order reversal.

   Test: publish selected-lane DQS as raw, reversed, inverted, and
   inverted-reversed bytes.

   Result: not sufficient. For selected lane 4 the raw/reversed forms were both
   =0xff= and inverted forms were both =0x00=. For a saturated byte, reversal
   cannot recover the expected alternating DQS window. This does not rule out a
   deeper ISERDES phase/bit-order issue on lanes that show =0x2a=, but it rules
   out a trivial polarity or bit-order fix for the selected saturated lane.

4. MPR/DQ data is present on some lanes but the controller is analyzing the
   wrong byte lane.

   Test: add same-cycle selected-lane MPR DQ and all-lane burst-0 MPR DQ at the
   =COLLECT_DQS= sample point.

   Result: still suspect, but this run did not find a good MPR byte. The selected
   lane and all-lane burst-0 MPR pages were both all ones:
   =selected_mpr_dq=0xffffffffffffffff= and
   =all_lane_mpr_burst0=0xffffffffffffffff=. This points away from "only lane 0
   is wrong" and toward lane ordering, input interpretation, or the exact MPR
   sample timing. The current all-lane MPR page is only burst 0; it does not yet
   prove every burst on every lane is saturated.

5. XDC byte-lane grouping or physical pinout mismatch.

   Test: statically inspect the YPCB XDC lane grouping against the controller's
   byte-lane convention =ddr3_dq[8*n +: 8]= with =ddr3_dqs_p/n[n]=.

   Result: still open. The XDC is grouped in byte ranges that look physically
   coherent with the matching DQS pairs:

   - lane 0: DQ[0..7] near DQS0 AK16/AK17.
   - lane 1: DQ[8..15] near DQS1 AM17/AM18.
   - lane 2: DQ[16..23] near DQS2 AP21/AP22.
   - lane 3: DQ[24..31] near DQS3 AH20/AJ20.
   - lane 4: DQ[32..39] near DQS4 AK33/AL33.
   - lane 5: DQ[40..47] near DQS5 AG33/AH33.
   - lane 6: DQ[48..55] near DQS6 AE34/AF34.
   - lane 7: DQ[56..63] near DQS7 AE27/AE28.

   This makes an obvious range-level XDC error less likely, but it is not a
   schematic-level proof. A swapped byte-lane order, swapped DQS pair, or
   board-specific memory byte ordering can still produce exactly this kind of
   "some lanes have activity, selected lane saturates" result.

6. PHY/controller input interpretation mismatch.

   Test: statically inspect the PHY-to-controller packing.

   Result: still open and now high priority. The PHY maps DQ ISERDES =Q1..Q8= to
   burst slots 7 down to 0, while DQS maps =Q1..Q8= to lane bits 7 down to 0.
   The controller consumes selected DQS as
   =i_phy_iserdes_dqs[serdes_ratio*2*lane +: 8]= and selected MPR DQ as
   =i_phy_iserdes_data[((DQ_BITS*LANES)*burst + 8*lane) +: 8]=. The conventions
   are internally consistent by inspection, but the measured lower-lane =0x2a=
   pattern means the next useful test is an all-lane, all-burst MPR capture or a
   lane-selectable capture that can walk lanes 0..7 and compare DQS/MPR at the
   same sample point.

Current conclusion:

The useful measurement is no longer "try random timing fixes". DQS exists on
some lanes, selected lanes can saturate high, and all-lane MPR burst 0 also
captured all ones in the selected-lane run. The next measured probe should keep
using JTAG and should capture either all lanes across all eight MPR bursts, or
provide a JTAG-selected lane index so lanes 0..7 can be sampled one at a time
with DQS raw/reversed/inverted and MPR DQ from the same cycle.

** 2026-05-20 all-lane/all-burst MPR probe:

The remaining high-value suspects are byte-lane ordering/pinout and
PHY/controller input interpretation. The next measured probe therefore captures
all lanes across all eight MPR bursts at the same =COLLECT_DQS= sample point as
the selected-lane DQS page.

Implementation:

- Add controller debug page =o_debug8[511:0]=.
- Latch =i_phy_iserdes_data[511:0]= when
  =state_calibrate == COLLECT_DQS && delay_before_read_data == 0=.
- Widen YPCB USER1 JTAG from 448 bits to 960 bits.
- Decode the new page in =openocd_ypcb_debug.tcl= as all-lane MPR bursts 7..0.
- Keep the previous selected-lane DQS, selected-lane MPR, all-lane DQS, and DQS
  counter pages unchanged.

Expected interpretation:

- If every burst/lane is all ones, focus on MPR read timing, DQ input biasing, or
  PHY sampling around the MPR read window.
- If some lanes show an alternating or MPR-like byte while the selected lane is
  saturated, focus on byte-lane ordering, DQS-to-DQ grouping, or board pinout.
- If only some bursts show useful values, focus on ISERDES burst ordering,
  bitslip/read-pipe interpretation, or sample-cycle selection.
- If the all-burst page disagrees with the existing selected-lane page, debug the
  instrumentation indexing before changing DDR3 behavior.

Execution invariant:

Use one lock for the whole hardware transaction: acquire lock, program bitstream,
read JTAG samples, release lock.

Measured result for commit =70c3ca1=:

- =make openxc7= passed post-route timing: =i_controller_clk= was 89.90 MHz
  against the 83.333 MHz target.
- The hardware run used one locked transaction: acquire lock, program bitstream,
  read eight JTAG samples, release lock.
- The useful sample caught =state=4= (=ANALYZE_DQS=) with selected lane 1:
  - =pll_locked=1
  - =calib_complete=0
  - =dqs_lane=1=
  - =selected_collect_lane=1=
  - =selected_dqs_collect=0xff=
  - =selected_mpr_dq=0xffffffffffffffff=
  - all-lane MPR bursts:
    =b7=ffffffffffffffff b6=ffffffffffffffff b5=ffffffffffffffff b4=ffffffffffffffff=
    =b3=ffffffffffffffff b2=ffffffffffffffff b1=ffffffffffffffff b0=ffffffffffffffff=
  - =all_lane_mpr_burst0=0xffffffffffffffff=
  - =all_lane_dqs=0x00ff00ff0000ffff=
  - =dqs_store=0xf2a9ffff=
  - =dqs_start_index=4=
  - =dqs_start_index_stored=17=
  - =dqs_start_index_repeat=1=

Interpretation:

- This run did not find a valid MPR byte on any lane or burst at the current
  capture point. The whole 512-bit MPR/DQ sample was all ones while the all-lane
  DQS snapshot remained nonzero/uneven.
- That weakens the simple "selected lane is wrong but another lane has good MPR
  data" hypothesis for this exact sample point.
- The next measured split is between MPR read/sample timing and PHY input
  interpretation: capture the same all-lane/all-burst MPR page at several
  adjacent controller cycles around =delay_before_read_data == 0=, or add a
  small time-indexed ring buffer for the MPR/DQS sample window.

** 2026-05-20 OpenXC7 warning analysis:

Goal: analyze every warning class in the YPCB OpenXC7 flow and either address it
or explicitly ignore/defer it.

Captured baseline:

- Forced full rebuild command: touch top Verilog, then =make openxc7= with output
  redirected to =/tmp/ypcb_openxc7_warnings.log=.
- Build completed and routed =i_controller_clk= at 89.90 MHz, passing the
  83.333 MHz target.
- Yosys reported =4494= warnings. nextpnr reported no final route warnings.

Warning classes:

1. Floating-point primitive parameters.

   Examples:
   - =clk_wiz.v=: PLL phase/duty/period parameters.
   - =rtl/ddr3_phy.v=: IDELAYE2 =REFCLK_FREQUENCY= and DQS =IDELAY_VALUE=.

   Decision: ignore for now. These are Xilinx primitive/model parameter typing
   messages from Yosys converting real-valued parameters. They do not indicate a
   missing net or changed logic by themselves. Fixing them globally would touch
   primitive instantiations and should be validated separately.

2. Memory replaced with list of registers.

   Examples: =stage2_data=, =stage2_dm=, =o_wb_data_q=, delay counters, command
   pipeline arrays.

   Decision: ignore. This is the expected synthesis representation for small RTL
   register arrays used with per-lane indexing. It is noisy but not a correctness
   warning.

3. OSERDESE2 32-bit-to-1-bit port resizing from unsized zero tie-offs.

   Examples: =SHIFTIN1(0)=, =SHIFTIN2(0)=, =T1(0)=, =T2(0)=, =T3(0)=,
   =T4(0)=, =TBYTEIN(0)=.

   Decision: valid fix, deferred. A trial changed these to =1'b0= and removed
   the associated width warnings, but the forced full route then missed timing
   at 82.58 MHz. Since this is warning hygiene, not a functional fix, do not keep
   the source edit while the 960-bit debug probe is already close to timing.

4. Top-level 1-bit input width resizing from unsized constants.

   Examples: =i_wb_cyc(1)=, =i_wb_stb(0)=, =i_wb_we(0)=, =i_wb2_cyc(0)=,
   =i_wb2_stb(0)=, =i_wb2_we(0)=, =i_user_self_refresh(0)=.

   Decision: valid fix, deferred for the same timing reason as class 3. Use
   explicitly sized =1'b0=/=1'b1= when the wide debug probe is narrowed or when a
   timing-clean warning pass is made.

5. Top-level dynamic bit write warning on =debug_state_seen[o_debug1[4:0]] <= 1'b1=.

   Decision: valid fix, deferred. Rewriting as an ORed one-hot update removed the
   warning but was part of the same source cleanup netlist that missed timing.
   The current warning is confined to debug instrumentation and does not affect
   DDR3 behavior.

6. Controller undriven generated wires and multiple-driver generated mux warnings.

   Examples point into dynamic indexed calibration logic around:
   - stage2 lane realignment assignments near =ddr3_controller.v:1088=,
     =1119=, and =1160=.
   - read-pipe/data-start arrays and =dq_target_index= generated mem2bits wires.
   - BIST byte-write assignment to =calib_data= around =ddr3_controller.v:3193=.

   Decision: defer, not ignore permanently. These warnings are the only class
   that could hide a real synthesis semantic issue. They come from existing RTL
   patterns with dynamic array indexing, variable part selects, and multiple
   nonblocking writes to parts of the same vector. Refactoring them safely is a
   separate experiment because it touches calibration/BIST behavior and must be
   verified against hardware.

7. =ABC: The network is combinational=.

   Decision: ignore. This is an ABC informational warning during logic mapping,
   not a specific RTL hazard in this flow.

8. nextpnr placement timing =FAIL= before route.

   Decision: ignore if post-route timing passes. The flow already gates on the
   final post-route frequency. The baseline warning-capture build recovered from
   placement =FAIL= to post-route =PASS=.

Attempted cleanup result:

- Trial fixes for classes 3, 4, and 5 reduced Yosys warnings from =4494= to
  =3794=.
- The forced full route missed target at 82.58 MHz versus 83.333 MHz.
- Source edits from that trial were backed out. Keep the warning ledger and defer
  these cleanup edits until the debug payload is smaller or timing margin is
  available.

Next action:

Do not random-fix all warnings in the active calibration-debug bitstream. The
safe measured path is to first narrow or time-multiplex the 960-bit JTAG payload,
then reapply the low-risk width/tie-off cleanup and rerun the warning ledger.


** 2026-05-20 Vivado comparison experiment:

Goal: determine whether the calibration failure is specific to the OpenXC7
implementation or persists with Vivado using the same RTL, XDC, and USER1 JTAG
debug payload.

Vivado environment:

- Setup script: =/home/roland/Vivado/2025.2.1/Vivado/settings64.sh=.
- Version: =vivado v2025.2.1 (64-bit)=.

Build result:

- Command: =source /home/roland/Vivado/2025.2.1/Vivado/settings64.sh && make vivado > /tmp/ypcb_vivado_make.log 2>&1=.
- Output bitstream: =build/ypcb_00338_1p1_ddr3_vivado.bit=.
- Vivado completed =write_bitstream= successfully.
- Final route timing was clean: =WNS=0.495=, =TNS=0.000=, =WHS=0.050=,
  =THS=0.000=.
- The end of =build/top.log= reported =0 Warnings, 0 Critical Warnings and
  0 Errors= for bitstream generation.

Hardware transaction:

The Vivado bitstream was programmed and sampled with one lock covering the whole
attributable transaction:

1. Acquire =/tmp/ypcb-hs3-210299BF3824.lock=.
2. Program =build/ypcb_00338_1p1_ddr3_vivado.bit=.
3. Run 16 OpenOCD USER1 samples.
4. Release the lock.

Measured result:

- Programming completed and JTAG DONE readback reported =done 1=.
- USER1 debug magic remained valid: =magic=0xd3b5=.
- =pll_locked=1=.
- =calib_complete=0=.
- All 16 samples were stable at =state=17=.
- =dqs_state=17=.
- =dqs_lane=7= and =selected_collect_lane=7=.
- =dqs_current=0xaa=.
- =dqs_store=0x0002a800=.
- =dqs_start_index=9= and =dqs_start_index_stored=9=.
- =dqs_start_index_repeat=0=.
- =all_lane_dqs=0xaaaaaaaaaaaaaaaa=.
- =dqs_nonzero_nibbles=0xffffffff= and
  =dqs_transition_nibbles=0xffffffff=.
- All-lane/all-burst MPR page was all zero:
  =b7=0000000000000000 b6=0000000000000000 b5=0000000000000000 b4=0000000000000000=
  =b3=0000000000000000 b2=0000000000000000 b1=0000000000000000 b0=0000000000000000=.
- =selected_mpr_dq=0x0000000000000000=.
- =selected_dqs_collect=0x00=, reversed =0x00=, inverted =0xff=, and
  inverted-reversed =0xff=.
- BIST did not start: =bist_progress=0x00=, =correct_low=0=, =wrong_low=0=.

Interpretation:

- Vivado does not make the design calibrate on this board. That weakens the
  hypothesis that the failure is only an OpenXC7 implementation artifact.
- Vivado does change the measured calibration behavior substantially. OpenXC7
  produced uneven all-lane DQS snapshots such as =0x00ff00ff0000ffff= and MPR
  samples that were all ones at the captured point. Vivado produced a clean
  all-lane DQS pattern =0xaaaaaaaaaaaaaaaa=, but the all-lane MPR page was all
  zeros.
- The JTAG debug path survived Vivado synthesis/implementation, so the USER1
  instrumentation is usable for cross-tool comparison.
- The next measured split should focus on the controller state represented by
  =state=17= and why Vivado has good-looking DQS activity while MPR DQ is all
  zero. This points at MPR command/data timing, read-data enable/sample-cycle
  selection, or PHY/controller interpretation rather than a simple missing-DQS
  failure.


** 2026-05-20 placement audit and Vivado BIST-state probe:

Goal: use Vivado as a same-board reference and similar examples as structural
references, then explain what Vivado's stable =state=17= means.

Implemented commits:

- =67516a6= adds Vivado routed reports and a DDR placement audit helper.
- =3ada8c5= changes the audit to summarize byte lanes and DQS sites.
- =8aa1249= adds a =BURST_WRITE= / BIST request debug overlay and decodes it
  in the OpenOCD script.

Vivado report result:

- Report-enabled =make -B vivado= completed.
- Final Vivado timing after the BIST probe was clean:
  =WNS=0.522=, =TNS=0.000=, =WHS=0.046=, =THS=0.000=.
- Bitstream generation completed with =0 Warnings, 0 Critical Warnings and
  0 Errors= in the final Vivado stages.
- Generated local artifacts include the routed DCP, timing/utilization/clock/IO
  reports, and =build/ypcb_00338_1p1_ddr3_vivado_ddr_cells.tsv=. These are
  measurement artifacts and should not be committed.

Placement audit result:

- YPCB OpenXC7 and YPCB Vivado have matching DDR primitive counts at the logical
  level: =64 IOBUF=, =8 IOBUFDS=, =72 IDELAYE2=, =72 ISERDESE2=,
  =97 OSERDESE2=, =1 PLLE2_ADV=.
- The YPCB byte-lane structure is coherent by inspection: each lane has
  =8 DQ IOBUF/IDELAY/ISERDES/OSERDES= plus =1 DQS IOBUFDS/IDELAY/ISERDES/OSERDES=.
- Similar examples show the same per-byte-lane structure for their lane count.
  This does not prove the board pinout is correct, but it weakens the idea that
  YPCB has an obvious missing primitive or malformed byte-lane generation.
- Vivado places each YPCB byte lane into compact I/O site spans with the DQS site
  inside the same span:
  - lane 0: DQ =Y13..Y22=, DQS =Y20=
  - lane 1: DQ =Y25..Y36=, DQS =Y32=
  - lane 2: DQ =Y39..Y48=, DQS =Y44=
  - lane 3: DQ =Y1..Y10=, DQS =Y8=
  - lane 4: DQ =Y114..Y123=, DQS =Y120=
  - lane 5: DQ =Y125..Y136=, DQS =Y132=
  - lane 6: DQ =Y139..Y148=, DQS =Y144=
  - lane 7: DQ =Y51..Y60=, DQS =Y58=

Vivado BIST-state probe result for commit =8aa1249=:

- The hardware run used one locked transaction: acquire HS3 lock, program the
  Vivado bitstream, sample USER1 16 times, release lock.
- USER1 remained valid: =magic=0xd3b5=.
- =pll_locked=1= and =calib_complete=0=.
- All samples were =state=17= (=BURST_WRITE=), but the state was not wedged.
- The BIST request overlay showed accepted write requests:
  - =calib_stb=1=
  - =o_wb_stall_calib=0= for most samples
  - =o_wb_ack_uncalibrated=1= for most samples
  - =we=1= and =aux=0x2=, matching BIST write requests
  - sampled =write_addr= advanced from =0x00095ee= to =0x047471f=
  - sampled =calib_addr= tracked =write_addr=
  - =read_addr= stayed =0= because =BURST_READ= had not started yet
- One sample had =o_wb_stall_calib=1=, which is expected transient backpressure,
  not a permanent stall.

Updated interpretation:

- Vivado reaching =state=17= is not evidence of a new failure at =BURST_WRITE=.
  It is actively streaming the first BIST write phase over the full configured
  memory address space.
- The earlier conclusion should be refined: Vivado appears to pass early DQS and
  write/read calibration well enough to begin BIST, while OpenXC7 remains stuck
  much earlier around =MPR_READ/COLLECT_DQS/ANALYZE_DQS=.
- Placement comparison did not find a structural byte-lane generation problem.
  The remaining OpenXC7-specific suspect is still implementation of the early
  DQS/MPR sample path, not BIST.

Next measured action:

Build a deliberately short BIST or calibration-only variant for Vivado and
OpenXC7. For Vivado, this should confirm whether the design reaches
=DONE_CALIBRATE= when the BIST write/read sweep is shortened. For OpenXC7, it
will not mask the early failure if the design still cannot pass =ANALYZE_DQS=.

** 2026-05-20 long Vivado BIST wait:

Goal: test the hypothesis that the Vivado bitstream only needed more time after
entering =BURST_WRITE=.

Command discipline:

- The run used one locked hardware transaction:
  acquire =/tmp/ypcb-hs3-210299BF3824.lock=, source Vivado, program
  =build/ypcb_00338_1p1_ddr3_vivado.bit=, sample USER1, release lock.
- OpenOCD captured =360= samples with =250 ms= delay, about =90 s= of post-program
  JTAG observation.
- Raw log: =/tmp/ypcb_vivado_long_bist_jtag.log=.

Result:

- =magic=0xd3b5= remained valid through the run.
- =pll_locked=1= throughout.
- =calib_complete=0= for all =360= samples.
- Top-level =state=17= (=BURST_WRITE=) for all samples.
- No samples reached =state=18..23=, =DONE_CALIBRATE=, =finish=1=, or =done=1=.
- =read_addr= stayed =0= for all samples.
- Sampled =write_addr= reached as high as =0x07fc2da=, then later samples showed
  lower addresses again, with the final sample at =0x01b1bc4=.
- The debug "seen" flags recorded =burst_read:1= in =332/360= samples, while the
  controller state and request fields still showed active BIST write requests.
  Treat that as evidence that the BIST/debug overlay observed internal read-path
  activity or stale/latched phase flags, not as proof that the controller had
  entered the =BURST_READ= state.
- =wrong_low= became =0xaa= after the first =28= samples, so the BIST probe is
  seeing mismatch information during this long write/test window.

Interpretation:

- This run does not show that the Vivado bitstream reaches
  =DONE_CALIBRATE= on the full BIST configuration.
- It also does not look like a simple "wait a few more seconds" issue. After
  about =90 s=, the sampled state was still =BURST_WRITE= and =calib_complete=0=.
- Vivado still teaches something useful: unlike OpenXC7, it passes the early
  DQS/MPR calibration states and reaches the BIST write path. The OpenXC7
  failure remains earlier, around =ANALYZE_DQS=.
- The full BIST path is too long or too ambiguous for quick calibration
  attribution. The next measured experiment should shorten or bypass BIST so
  both flows can answer one question cleanly: "did calibration complete?"

Next measured action:

Add a controlled short-BIST or calibration-only build option, commit it as its
own experiment, and test both Vivado and OpenXC7 under the same locked
program-and-read transaction. Keep the full BIST build as the reference, but do
not use it as the primary answer for calibration completion timing.

** 2026-05-20 calibration-only oracle experiment:

Goal: separate early calibration from the long full-BIST path, then use Vivado
as a same-board oracle for OpenXC7.

Implementation commits:

- =8d71283= adds YPCB =CALIB_ONLY= and =SHORT_BIST= build modes and explicit
  JTAG decode for BIST mode, initial/final calibration done, and reset causes.
- =163e090= fixes debug-only address slices so shortened/optimized counters do
  not create out-of-bounds synthesis warnings.

Build results:

- =make vivado_calib_only= produced
  =build/ypcb_00338_1p1_ddr3_vivado_calib_only.bit=.
- Vivado timing passed: WNS =0.537 ns=, TNS =0.000=, WHS =0.084 ns=,
  THS =0.000=.
- =make openxc7_calib_only= produced
  =ypcb_00338_1p1_ddr3_calib_only_openxc7.bit=.
- OpenXC7 post-route timing passed =--freq 83.333= with =i_controller_clk=
  max frequency =84.45 MHz=.

Locked hardware measurements:

- Vivado command discipline: one lock covered program plus =80= JTAG samples at
  =250 ms= delay. Raw log: =/tmp/ypcb_vivado_calib_only_jtag.log=.
- OpenXC7 command discipline: one lock covered program plus =80= JTAG samples at
  =250 ms= delay. Raw log: =/tmp/ypcb_openxc7_calib_only_jtag.log=.

Vivado =CALIB_ONLY= result:

- All =80= samples were at current =state=23= (=DONE_CALIBRATE=).
- =calib_complete=1= for all samples.
- =bist_flags={mode:0 initial_done:1 final_done:1 reset_from_calibrate:0 reset_from_test:0 short:0}=.
- DQS/MPR oracle values after completion were stable:
  - =dqs_store=0x0002a800=
  - =dqs_start_index=9= and =dqs_start_index_stored=9=
  - =selected_collect_lane=0=
  - =selected_mpr_dq=0x3d3d3d3d3d3d3d3d=
  - =selected_dqs_collect=0x10= in =76/80= samples and =0x12= in =4/80=.

OpenXC7 =CALIB_ONLY= result:

- Current states observed across =80= samples:
  - =state=2= (=MPR_READ=): =18= samples
  - =state=3= (=COLLECT_DQS=): =8= samples
  - =state=4= (=ANALYZE_DQS=): =54= samples
- =calib_complete=0= for all samples.
- =calib_flags={mode:0 limit_bits:0 initial_done:0 final_done:0 reset_from_calibrate:0 reset_from_test:0}=.
- OpenXC7 selected-lane capture stayed saturated:
  - =dqs_store=0xffffffff=
  - =dqs_start_index_stored=39=
  - =selected_collect_lane=1=
  - =selected_dqs_collect=0xff=
  - =selected_mpr_dq=0xffffffffffffffff=

Interpretation:

- Vivado does calibrate on YPCB when BIST is bypassed. The previous long full-BIST
  result was not a calibration failure; it was an ambiguous full-BIST-duration
  measurement.
- OpenXC7 still fails before calibration completion even with BIST removed. The
  first useful divergence remains early MPR/DQS capture and analysis, not BIST.
- The highest-value next comparison is now phase-local: compare Vivado and
  OpenXC7 primitive placement and routing for the selected DQS/DQ lane around
  =MPR_READ=, =COLLECT_DQS=, and =ANALYZE_DQS=, using other Xilinx board
  examples as structural references.

Next measured action:

Build and test =SHORT_BIST= only after the OpenXC7 early calibration divergence
has a targeted hypothesis. For now, do not spend time on BIST behavior; it is
behind the failing boundary in OpenXC7.

** 2026-05-20 Vivado placement-oracle experiment:

Goal: test whether OpenXC7's calibration failure is explained by gross placement
of the DDR input-capture byte lanes, and decide whether Vivado-derived pre-place
constraints are a high-value next experiment.

Plan documented before execution:

- Use Vivado =CALIB_ONLY= as the known-good same-board placement oracle.
- Extract final OpenXC7 placement with a post-route JSON, not the pre-place Yosys
  JSON.
- Compare only the failing phase's resources first: DQ/DQS IOB, IDELAY,
  ISERDES, and OSERDES by byte lane.
- If OpenXC7 differs materially from Vivado for the failing selected lane, create
  a narrow pre-place experiment for that byte lane only. If OpenXC7 already
  matches Vivado, do not spend an experiment on no-op placement constraints.

Executed measurement:

- Ran nextpnr with =--write ypcb_00338_1p1_ddr3_calib_only_routed.json= for the
  OpenXC7 =CALIB_ONLY= design.
- The run matched the prior routed timing boundary: =i_controller_clk= max
  frequency =84.45 MHz=, passing the =83.333 MHz= target.
- Compared that routed JSON against
  =build/ypcb_00338_1p1_ddr3_vivado_calib_only_ddr_cells.tsv=.

Result:

- Every byte lane has the same DQ IOB/IDELAY/ILOGIC/OLOGIC span in OpenXC7 and
  Vivado:
  - lane 0: =Y13..Y22=, DQS =Y20=
  - lane 1: =Y25..Y36=, DQS =Y32=
  - lane 2: =Y39..Y48=, DQS =Y44=
  - lane 3: =Y1..Y10=, DQS =Y8=
  - lane 4: =Y114..Y123=, DQS =Y120=
  - lane 5: =Y125..Y136=, DQS =Y132=
  - lane 6: =Y139..Y148=, DQS =Y144=
  - lane 7: =Y51..Y60=, DQS =Y58=
- For lanes 0 and 1, every DQ bit's =IOBUF=, =IDELAYE2=, =ISERDESE2=, and
  =OSERDESE2= site matched Vivado exactly.
- OpenXC7 reports DQS differential IOBs as both P/N physical sites, e.g.
  lane 1 =IOB_X0Y31,IOB_X0Y32=, while Vivado's TSV reports the master site
  =IOB_X0Y32=. The associated DQS =IDELAY=, =ILOGIC=, and =OLOGIC= site is the
  same in both flows.

Interpretation:

- Vivado-derived pre-place constraints for the DDR byte-lane input capture sites
  are not the next useful experiment: OpenXC7 is already placing those resources
  where Vivado places them.
- The failure is now more likely in primitive configuration, inversion/polarity,
  ISERDES bit ordering, IDELAY behavior, or route/resource semantics than in
  gross byte-lane site placement.
- The next oracle-guided comparison should inspect configured properties/FASM for
  lane 0 and lane 1 DQS/DQ =IDELAYE2= and =ISERDESE2= instances, then form one
  targeted experiment around the first concrete mismatch.

Next measured action:

Use the Vivado routed DCP to report DQS/DQ primitive properties for lanes 0 and
1, and compare them to OpenXC7 routed JSON/FASM for the same cells. Focus first
on =ISERDESE2= data width/interface mode/clock inversion/bit order and
=IDELAYE2= delay mode/load/value fields.


** 2026-05-20 DQS IDELAY primitive-oracle experiment:

Goal: test the first concrete Vivado/OpenXC7 primitive-configuration mismatch,
not a broad placement guess.

Measured oracle comparison:

- Vivado routed =CALIB_ONLY= DCP reports =IDELAY_VALUE=10= for lane 0 and lane 1
  DQS =IDELAYE2= instances.
- OpenXC7 routed JSON preserves the RTL-derived fractional parameter as
  =IDELAY_VALUE=9.600000= for the same DQS =IDELAYE2= instances.
- OpenXC7 FASM encodes the DQS initial tap as =IDELAY_VALUE[4:0] = 5'b01001=,
  i.e. tap =9=, for all eight DQS lanes.
- The inspected DQ and DQS =ISERDESE2= parameters match the Vivado oracle for the
  checked lanes: =DATA_RATE=DDR=, =DATA_WIDTH=8=, =INTERFACE_TYPE=NETWORKING=,
  =IOBDELAY=IFD=, =SERDES_MODE=MASTER=, and =IS_CLKB_INVERTED=1=.

Hypothesis:

- The open flow truncates the fractional DQS IDELAY parameter to tap =9=, while
  Vivado uses tap =10=. This may move the MPR/DQS sample point enough to produce
  the observed saturated DQS/MPR capture in OpenXC7.
- This is an oracle-guided experiment, not a random delay sweep: it tests the
  exact primitive value where Vivado and OpenXC7 diverge.

Experiment to execute:

- Change =DQS_IDELAY_TAP= to an integer rounded expression for the input DQS
  delay. For =DDR3_CLK_PERIOD=3000 ps=, the computed quarter-cycle delay is
  =9.6= taps, so the rounded integer is tap =10=.
- Rebuild =openxc7_calib_only=.
- Confirm the new OpenXC7 FASM encodes DQS =IDELAY_VALUE[4:0] = 5'b01010=.
- Program and sample under one hardware lock:
  =acquire lock -> program rounded-tap OpenXC7 bitstream -> read JTAG samples -> release lock=.
- Interpret the result against the prior baseline:
  - reaches =DONE_CALIBRATE=: DQS initial tap truncation was a calibration blocker.
  - stays at =ANALYZE_DQS= but DQS/MPR values change: tap mismatch matters but is
    not sufficient.
  - remains saturated with the same values: continue with ISERDES bit order,
    DQS polarity, route semantics, or controller input interpretation.

Executed result:

- Rebuilt =openxc7_calib_only= after rounding =DQS_IDELAY_TAP=. The final routed
  OpenXC7 timing still passed: =i_controller_clk= max frequency =84.45 MHz= at
  the =83.333 MHz= target.
- The original DQS =IDELAY_VALUE=9.600000= synthesis warning disappeared; the
  remaining floating-point warnings are for unrelated primitive fields such as
  =REFCLK_FREQUENCY=200.000000=.
- The rebuilt FASM now encodes all eight DQS lanes as
  =IDELAY_VALUE[4:0] = 5'b01010=, matching Vivado's tap =10= oracle.
- Programmed and sampled under one hardware lock. Raw log:
  =/tmp/ypcb_openxc7_calib_only_dqs_tap10_jtag.log=.
- Hardware result across =80= samples:
  - =state=2= (=MPR_READ=): =10= samples
  - =state=3= (=COLLECT_DQS=): =7= samples
  - =state=4= (=ANALYZE_DQS=): =63= samples
  - =calib_complete=0= for all =80= samples
- The selected-lane failure signature did not improve:
  - =selected_collect_lane=1=
  - =dqs_store=0xffffffff=
  - =dqs_start_index_stored=39=
  - =selected_dqs_collect=0xff=
  - =selected_mpr_dq=0xffffffffffffffff=

Interpretation after execution:

- The DQS tap 9-vs-10 mismatch was real, but it is not the primary calibration
  blocker for OpenXC7 on YPCB. Matching Vivado's initial DQS IDELAY tap leaves
  the same saturated DQS/MPR capture signature.
- Continue with the remaining oracle-guided suspects: DQS polarity/input
  interpretation, ISERDES bit order, route/resource semantics around the DQS
  differential input path, or controller interpretation of the per-lane capture.
- A useful next experiment is to use the Vivado oracle to compare the DQS input
  net/route features and IOB differential polarity features for lane 1 first,
  since the selected lane is consistently lane 1 in the failing OpenXC7 samples.


** 2026-05-20 Lane 1 DQS polarity/route/ISERDES oracle experiment:

Goal: test the next measured suspect after the DQS tap experiment: whether
OpenXC7's failing selected lane, lane 1, differs from Vivado in DQS P/N polarity,
local DQS input route, or ISERDES input interpretation.

Plan documented before execution:

- Use Vivado =CALIB_ONLY= as the known-good oracle.
- Inspect lane 1 only first, because OpenXC7 consistently selects lane 1 at the
  failing =ANALYZE_DQS= point.
- Compare:
  - DQS P/N port-to-site mapping.
  - =IOBUFDS_dqs.O -> IDELAYE2_dqs.IDATAIN= route.
  - =IDELAYE2_dqs.DATAOUT -> ISERDESE2_dqs.DDLY= route.
  - =ISERDESE2_dqs= mode, clock inversion, DDLY use, and Q bit ordering.
- Only create a new RTL/constraint experiment if this comparison exposes a
  concrete mismatch.

Executed measurement:

- Extracted lane 1 DQS properties and local routes from Vivado routed DCP into
  =/tmp/ypcb_vivado_lane1_dqs_oracle.txt=.
- Regenerated current tap-10 OpenXC7 route artifacts with:
  =ypcb_00338_1p1_ddr3_calib_only_tap10_routed.json= and
  =ypcb_00338_1p1_ddr3_calib_only_lane1_oracle.fasm=.
- The regenerated OpenXC7 route again passed =--freq 83.333= after routing:
  =i_controller_clk= max frequency =84.45 MHz=.

Static comparison result:

- DQS P/N mapping matches the Vivado oracle:
  - =ddr3_dqs_p[1]= is package pin =AM17= at =IOB_X0Y32=.
  - =ddr3_dqs_n[1]= is package pin =AM18= at =IOB_X0Y31=.
  - Vivado uses =IOBUFDS_dqs= at =IOB_X0Y32= with =IOB33M.INBUF_EN=.
  - OpenXC7 uses the same master input buffer: =IOB_X0Y32/IOB33M/INBUF_EN=,
    with the N-side output buffer at =IOB_X0Y31=.
- DQS input termination/polarity features match at the level visible in the
  routed artifacts:
  - Vivado: =DIFF_SSTL15=, =SLEW=FAST=, =IN_TERM=UNTUNED_SPLIT_40=,
    =DIFF_TERM=0=, =IBUF_LOW_PWR=0=.
  - OpenXC7 FASM for lane 1 includes =IN_DIFF= on both IOB sites,
    =IN_TERM.UNTUNED_SPLIT_40=, =SLEW.FAST=, =PULLTYPE.NONE=, and =OUT_DIFF=.
- The local DQS input route matches the intended dedicated path:
  - Vivado =IOBUFDS_dqs/O -> IDELAYE2_dqs/IDATAIN= route is
    =IOB_IBUF0 LIOI_I0 LIOI_IDELAY0_IDATAIN=.
  - OpenXC7 connects =IOBUFDS_dqs$intcell$IBUF.OUT= directly to
    =IDELAYE2_dqs.IDATAIN= on the same =X0Y32= byte site.
  - Vivado =IDELAYE2_dqs/DATAOUT -> ISERDESE2_dqs/DDLY= route is
    =LIOI_IDELAY0_DATAOUT LIOI_ILOGIC0_DDLY=.
  - OpenXC7 connects =IDELAYE2_dqs.DATAOUT= directly to
    =ISERDESE2_dqs.DDLY= on the same =X0Y32= byte site.
- =ISERDESE2_dqs= configuration matches the Vivado oracle for the meaningful
  fields:
  - =DATA_RATE=DDR=
  - =DATA_WIDTH=8=
  - =INTERFACE_TYPE=NETWORKING=
  - =IOBDELAY=IFD=
  - =NUM_CE=1=
  - =IS_CLKB_INVERTED=1=
  - =DDLY= is used, not direct =D=.
  - Q ordering is the RTL-intended reverse mapping: =Q1 -> i_phy_iserdes_dqs[15]=
    through =Q8 -> i_phy_iserdes_dqs[8]=.
- Vivado ties =DYNCLKSEL= and =DYNCLKDIVSEL= high, while OpenXC7 omits those
  ports in the routed JSON. This is not treated as actionable because Vivado also
  reports =DYN_CLK_INV_EN=FALSE= and =DYN_CLKDIV_INV_EN=FALSE= for the same cell,
  so the dynamic inversion inputs are disabled.

Dynamic comparison context:

- Vivado =CALIB_ONLY= reaches =DONE_CALIBRATE= and reports a stable selected
  lane 0 post-calibration capture, usually =selected_dqs_collect=0x10= and
  =selected_mpr_dq=0x3d3d3d3d3d3d3d3d=.
- OpenXC7 tap-10 still fails at =ANALYZE_DQS= with selected lane 1 saturated:
  =selected_dqs_collect=0xff= and
  =selected_mpr_dq=0xffffffffffffffff=.
- The common =all_lane_dqs= value seen in both logs is usually
  =0x00ff00ff0000ffff=, so a simple controller-side inversion/reversal of the
  selected 8-bit DQS byte is not enough: OpenXC7's selected lane has no observed
  DQS transitions to analyze.

Interpretation after execution:

- Lane 1 DQS static implementation now matches Vivado closely enough that a
  broad P/N swap, pre-place constraint, or ISERDES mode edit would be a random
  fix rather than a measured experiment.
- The remaining useful suspect is lower than this netlist-level comparison:
  OpenXC7 bitstream feature semantics for the 7-series IOB/ILOGIC/IDELAY path,
  or a runtime timing/clocking interaction that changes what the same-looking
  lane 1 path captures.

Next measured action:

- Use a dynamic oracle instead of another static placement/property comparison.
  The best next probe is a small debug build that latches the raw lane 1 DQS
  ISERDES byte and lane 1 MPR DQ data at the exact =COLLECT_DQS= sample cycle,
  before =ANALYZE_DQS= compresses it into =dqs_store=. If possible, latch the
  same point for all lanes so Vivado and OpenXC7 can be compared sample-for-
  sample rather than only after Vivado has completed calibration.


** 2026-05-20 COLLECT_DQS dynamic lane-1 probe:

Goal: compare Vivado and OpenXC7 at the exact dynamic sample point before
=ANALYZE_DQS= compresses DQS history into =dqs_store=.

Plan documented before execution:

- Keep the calibration algorithm unchanged.
- Reuse the existing 960-bit JTAG/BSCAN debug payload.
- Change =o_debug3= from a live all-lane DQS snapshot to the all-lane DQS byte
  vector latched at =state_calibrate == COLLECT_DQS && delay_before_read_data == 0=.
- Keep the existing all-lane MPR DQ capture, which is already latched at the same
  =COLLECT_DQS= sample point.
- Update =openocd_ypcb_debug.tcl= to print explicit lane 1 values from the same
  latched sample:
  - =lane1_collect_dqs= from the latched all-lane DQS vector.
  - =lane1_mpr_dq= reconstructed from the lane 1 byte in each of the eight MPR
    bursts.
- Interpret results:
  - Vivado lane 1 DQS/MPR healthy but OpenXC7 saturated: open-flow runtime
    input capture/bitstream semantics remain suspect.
  - Both flows show lane 1 saturation at this exact point: lane 1 may be a bad
    comparison target after Vivado has selected/calibrated lane 0, and the next
    probe should walk all lanes at the same sample point.
  - OpenXC7 DQS has transitions but MPR is saturated: focus on DQ/MPR data path.
  - OpenXC7 DQS is saturated and MPR is saturated: focus on DQS input capture,
    read command timing, or DQS gating/tri-state behavior.


Execution result:

- Commit under test: =a684eff= (=Add YPCB COLLECT_DQS lane1 probe=).
- =make openxc7_calib_only= completed and generated
  =ypcb_00338_1p1_ddr3_calib_only_openxc7.bit=.
- Post-route timing passed after routing:
  - =jtag_debug_inst.tck= max frequency 249.50 MHz.
  - =i_controller_clk= max frequency 85.06 MHz, passing the 83.333 MHz target.
- The locked hardware transaction programmed the bitstream and collected 80 JTAG
  samples before releasing =/tmp/ypcb-hs3-210299BF3824.lock=.
- State distribution from the 80 samples:
  - 14 samples in =state=2= (=MPR_READ=).
  - 12 samples in =state=3= (=COLLECT_DQS=).
  - 54 samples in =state=4= (=ANALYZE_DQS=).
  - 80 samples had =calib_complete=0=.
- Exact =COLLECT_DQS= latched fields:
  - =selected_collect_lane=1= in all 80 samples.
  - =lane1_collect_dqs=0xff= in all 80 samples.
  - =selected_dqs_collect=0xff= in all 80 samples.
  - =lane1_mpr_dq=0xffffffffffffffff= in all 80 samples.
  - =selected_mpr_dq=0xffffffffffffffff= in all 80 samples.
  - =dqs_store=0xffffffff= in all 80 samples.
  - =dqs_start_index_stored=39= in all 80 samples.
  - =all_lane_dqs_collect=0x00ff00ff0000ffff= for 76 of 80 samples, with four
    nearby variants during live cycling.

Interpretation after execution:

- The saturation is already present at the exact =COLLECT_DQS= sample point. It
  is not an artifact introduced later by =ANALYZE_DQS= compression into
  =dqs_store=.
- Lane 1 has both DQS and MPR DQ saturated high at the same sample point. That
  makes a pure DQS bit-order explanation weak for this OpenXC7 run; the sampled
  DQ data is also wrong before analysis.
- The all-lane DQS collect vector is still nonzero and structured, so this is
  not a global absence of DQS capture. The failing selected lane is seeing an
  all-high byte and all-high MPR data.
- The critical path report now names =debug_lane[1]= in the controller clock
  domain, but route still closes at 85.06 MHz. The debug mux is adding timing
  pressure, so keep subsequent probes narrow or explicitly register their debug
  outputs.

Next measured action:

- Run the same exact-sample decoder against the Vivado calibration-only oracle,
  or add a compile-time/JTAG-selected lane index so OpenXC7 and Vivado can walk
  lanes 0..7 at the identical =COLLECT_DQS= point. The decision criterion is
  whether Vivado sees non-saturated MPR/DQS on lane 1 at this point, or whether
  lane 1 is expected to be saturated until another lane is selected.
- If Vivado lane 1 is healthy while OpenXC7 lane 1 is all ones, focus on
  open-flow runtime input capture semantics around ILOGIC/IDELAY/ISERDES and DQ
  tri-state/read timing.
- If Vivado lane 1 also shows all ones at this exact point, treat fixed lane 1
  as a poor oracle target and move to all-lane/selected-lane comparison rather
  than a lane-specific fix.

* Targets

| Board             | Part              | Notes                                     |
|-------------------+-------------------+-------------------------------------------|
| ypcb_00338_1p1    | xc7k480tffg1156-2 | Main timing/BIST target                   |
| alinx_ax7325b     | xc7k325tffg900-2  | Similar Kintex-7 DDR3 demo                |
| enclustra_kx2_st1 | xc7k160tffg676-2  | Previously worked with a different chipdb |
| qmtech_kintex_7   | xc7k325tffg676-1  | Similar Kintex-7 DDR3 demo                |